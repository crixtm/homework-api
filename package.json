{
  "name": "api_example",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright + TypeScript API tests for MockAPI Dog Todos API",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "@types/node": "^22.10.1",
    "dotenv": "^17.2.1",
    "typescript": "^5.6.3"
  }
}